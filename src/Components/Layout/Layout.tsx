import React from 'react'
import Navbar from '../Navbar/Navbar'

export const Layout = ({}) => {
  return (
    <div className="layout">
            <Navbar />
        </div>
  )
}

export default Layout;
